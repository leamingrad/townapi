"""
    serializers.py

    This file decares the serializers for the api app
"""

"""
Add the following serializers:
- TownSerializer - Standard model serializer, packing the Town's fields, and
                   select parent fields into a flat structure. This should
                   produce something with the same data contained in a line of
                   the CSV file
- AggsSerializer - Custom serializer to pack the required aggregate data into
                   a JSON object
 """
